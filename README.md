# 交易所提现脚本

通过 EC2 执行的交易所提现管理工具，支持 Binance 和 Bybit。

## 功能

- 💰 **余额查询** - 查看交易所资产
- 📤 **提现** - 支持多地址、多网络提现
- 🔄 **账户划转** - 现货/资金账户互转
- 💵 **币安理财** - 活期申购/赎回
- 📊 **USDT-USDC 交易** - Bybit 订单簿交易

## 文件结构

```
├── main.py          # 主入口
├── utils.py         # 工具函数
├── balance.py       # 余额查询
├── withdraw_ops.py  # 提现操作
├── transfer.py      # 账户划转
├── earn.py          # 币安理财
├── trade.py         # USDT-USDC 交易
├── addresses.py     # 地址簿管理
├── config.json      # API 配置 (需自行创建)
└── addresses.json   # 地址簿 (需自行创建)
```

## 配置

1. 复制示例配置：
```bash
cp config.example.json config.json
cp addresses.example.json addresses.json
```

2. 编辑 `config.json`，填入 API 密钥

3. 配置 SSH：确保 `~/.ssh/config` 中有 EC2 主机配置

## 运行

```bash
python3 main.py
```

## 依赖

```bash
pip install -r requirements.txt
```
